{{ tfDoNotEditStamp }}

variable "dataflow_tmp_gcs_location" {
{{with variables.DataFlowTmpGCSLocation -}}
  default = "{{ . }}"
{{- end}}
}

variable "dataflow_template_gcs_path" {
{{with variables.DataFlowTemplateGCSPath -}}
  default = "{{ . }}"
{{- end}}
}

variable "dataflow_zone" {
{{with variables.DataFlowZone -}}
  default = "{{ . }}"
{{- end}}
}
